---
import Layout from '../components/layout/Layout.astro';
import ServiceDetailBlock from '../components/services/ServiceDetailBlock.astro';
import { servicesPageData } from '../content/servicesData'; // Importar los datos

const pageTitle = "Nuestros Servicios | LAYCE Construcciones";
const pageDescription = "Descubre la gama completa de servicios de construcción que LAYCE ofrece, desde diseño y planificación hasta ejecución y gestión de proyectos.";

// Para alternar fondos
const backgroundClasses = ['bg-white', 'bg-lightGray'];
---

<Layout title={pageTitle} description={pageDescription}>
  {/* Hero Section para la página de Servicios */}
  <section class="bg-primary text-white py-20 md:py-28 text-center scroll-animate util-fade-in util-duration-medium">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-semibold balance-text mb-4">
        Soluciones Integrales en Construcción
      </h1>
      <p class="text-lg md:text-xl lg:text-2xl text-blue-100 max-w-3xl mx-auto balance-text">
        En LAYCE, transformamos tus ideas en realidad con experiencia, innovación y un compromiso inquebrantable con la calidad en cada etapa.
      </p>
    </div>
  </section>

  {/* Listado de Servicios Detallados */}
  {servicesPageData.map((service, index) => (
    <ServiceDetailBlock 
        id={service.id}
        title={service.title}
        iconName={service.iconName}
        description={service.description}
        imageSrc={service.imageSrc}
        imageAlt={service.imageAlt}
        imagePosition={index % 2 === 0 ? 'left' : 'right'}
        bulletPoints={service.bulletPoints}
        cta={service.cta}
        backgroundColorClass={backgroundClasses[index % 2]}
    />
  ))}

</Layout>